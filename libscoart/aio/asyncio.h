#ifndef __LIBSCOA_ASYNCIO_H
#define __LIBSCOA_ASYNCIO_H

#include "../libscoa.h"

enum asyncio_event {
    // TODO: create non-blocking IO calls of open(), read() and write() and
    // specify their event type.
};

#endif // !__LIBSCOA_ASYNCIO_H
